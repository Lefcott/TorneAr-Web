export const getGreenColor = (hovered) => (hovered ? "#32ff65" : "#1dbe45");

export const getRedColor = (hovered) => (hovered ? "#ff5c71" : "#ff1a39");

export const getGrayColor = (hovered) => (hovered ? "#7a7a7a" : "#474747");
